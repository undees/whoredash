{
  "name": "whoredash",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build src/app.js --outdir=public --minify",
    "dev": "bun build src/app.js --outdir=public --watch",
    "test": "bun test",
    "serve": "bun serve.js"
  },
  "dependencies": {
    "lit": "^3.3.2"
  },
  "devDependencies": {
    "sharp": "^0.34.5"
  }
}
